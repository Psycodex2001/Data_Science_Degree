x10 = c(2, 43, 27, 96, 18)
rank(x10)

which.min(x10)

max(x10)
which.max(x10)

name = c("M", "A", "N", "O")
dist = c(0.8, 3.1, 2.8, 4.0)
tempo = c(10, 30, 40, 50)
tables = names(name, dist, tempo)
print(tables)

class(name, dist)
